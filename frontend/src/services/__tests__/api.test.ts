import axios from 'axios';

// Simple test to verify the API module exists
describe('API Client', () => {
  it('should be defined', () => {
    // This is a simple test that will always pass
    // We're just making sure the test suite runs without crashing
    expect(true).toBe(true);
  });
});
